# OpenOT conformance

Reusable report and stale-oracle helpers for OpenOT shared-memory harnesses.

This crate intentionally depends only on `open-ot-carriage` and `open-ot-shm`.
It does not depend on any PLC runtime implementation.
